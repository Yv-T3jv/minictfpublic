<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Customize your Devel Sixteen Prototype</title>
    <link rel="stylesheet" href="/styles.css">
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f4f4f9;
            color: #333;
            text-align: center;
        }

        header {
            background-color: #222;
            color: white;
            padding: 1em 0;
        }

        .container {
            margin: 2em auto;
            max-width: 800px;
            padding: 1em;
            background: white;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            border-radius: 8px;
        }

        img {
            max-width: 100%;
            height: auto;
            border-radius: 8px;
        }

        form {
            margin-top: 2em;
            text-align: left;
        }

        .form-section {
            margin-bottom: 1.5em;
        }

        .form-section h2 {
            border-bottom: 2px solid #222;
            padding-bottom: 0.5em;
            margin-bottom: 0.5em;
        }

        label {
            display: block;
            margin: 0.5em 0 0.2em;
            font-weight: bold;
        }

        input {
            width: 100%;
            padding: 0.5em;
            margin-bottom: 1em;
            border: 1px solid #ccc;
            border-radius: 4px;
            font-size: 1em;
        }

        button {
            display: block;
            width: 100%;
            padding: 0.7em;
            background-color: #222;
            color: white;
            border: none;
            border-radius: 4px;
            font-size: 1em;
            cursor: pointer;
        }

        button:hover {
            background-color: #444;
        }

        a {
            color: #222;
            text-decoration: none;
        }

        a:hover {
            text-decoration: underline;
        }
    </style>
</head>
<body>
    <header>
        <h1>Customize Your Devel Sixteen Prototype</h1>
        <p>Your IP is: <%= ip %> </p>
    </header>

    <div class="container">

        <img src="https://hips.hearstapps.com/hmg-prod/images/sixteen-1523909701.jpg" alt="Devel Sixteen Prototype">

        <form id="customize-form" action="/customize" method="POST">
            <div class="form-section">
                <h2>Stats</h2>
                <label for="top-speed">Top Speed (km/h)</label>
                <input type="number" id="top-speed" name="stats[top_speed]" required>

                <label for="weight">Weight (kg)</label>
                <input type="number" id="weight" name="stats[weight]" required>

                <label for="horsepower">Horsepower</label>
                <input type="number" id="horsepower" name="stats[horsepower]" required>
            </div>

            <div class="form-section">
                <h2>Appearance</h2>
                <label for="color">Color</label>
                <input type="text" id="color" name="appearance[color]" required>

                <label for="name">Name</label>
                <input type="text" id="name" name="appearance[name]" required>
            </div>

            <button type="submit">Submit</button>
        </form>
    </div>

    <script>
        document.getElementById('customize-form').addEventListener('submit', function(event) {
            event.preventDefault();

            const form = event.target;
            const formData = new FormData(form);

            const json = {
                stats: {
                    top_speed: formData.get('stats[top_speed]'),
                    weight: formData.get('stats[weight]'),
                    horsepower: formData.get('stats[horsepower]')
                },
                appearance: {
                    color: formData.get('appearance[color]'),
                    name: formData.get('appearance[name]')
                }
            };

            fetch(form.action, {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/json'
                },
                body: JSON.stringify(json)
            }).then(response => {
                if (response.ok) {
                    alert('We will be mailing your Devel Sixteen over shortly.');
                } else {
                    alert('Failed to submit form.');
                }
            }).catch(error => {
                console.error('Error submitting form:', error);
                alert('An error occurred while submitting the form.');
            });
        });
    </script>
</body>
</html>
